import { styled } from '@mui/material/styles';
import { DividerBorderTop } from '../shared';

export const TableFooterContainer = styled(DividerBorderTop)({
  paddingLeft: 16,
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'space-between'
});
