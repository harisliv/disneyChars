import { styled } from '@mui/material/styles';
import { DividerBorderTop } from '../shared';

export const CenteredDividerBorderTop = styled(DividerBorderTop)({
  padding: 8,
  display: 'flex',
  justifyContent: 'center'
});
