import { styled } from '@mui/material/styles';
import Box from '@mui/material/Box';

export const CardContainer = styled(Box)({
  boxShadow: '0px 2px 7px 0px #1919701f',
  borderRadius: 6,
  backgroundColor: '#fff',
  display: 'flex',
  flexDirection: 'column',
  height: '100%',
  overflow: 'hidden'
});
